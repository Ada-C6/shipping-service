class Quote
  def initialize(shipment_info_hash)
    @shipment = Shipment.new(shipment_info_hash)
    @ups_rate = 
  end


def find_rates
  ups.find_rates(@shipment.origin, @shipment.destination, a.)

end
end
